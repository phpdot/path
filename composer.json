{
    "name": "phpdot/path",
    "description": "Project-root discovery and named path resolution for PHPdot, configured via phpdot/config.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Omar Hamdan",
            "email": "omar@phpdot.com"
        }
    ],
    "keywords": [
        "path",
        "filesystem",
        "project-root",
        "config",
        "phpdot"
    ],
    "require": {
        "php": ">=8.5",
        "composer-runtime-api": "^2.2",
        "phpdot/config": "^0.1",
        "phpdot/package": "^0.1"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.94",
        "phpdot/container": "^0.1",
        "phpstan/phpstan": "^2.0",
        "phpstan/phpstan-strict-rules": "^2.0",
        "phpunit/phpunit": "^13.0"
    },
    "suggest": {
        "phpdot/container": "Autowires the container-attribute services and #[Config] DTOs declared in src (the attributes stay inert until reflected, so standalone consumers don't need it installed)."
    },
    "autoload": {
        "psr-4": {
            "PHPdot\\Path\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PHPdot\\Path\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse --memory-limit=256M",
        "cs-fix": "php-cs-fixer fix",
        "cs-check": "php-cs-fixer fix --dry-run --diff",
        "check": [
            "@test",
            "@analyse",
            "@cs-check"
        ]
    },
    "config": {
        "sort-packages": true
    },
    "support": {
        "source": "https://github.com/phpdot/path",
        "issues": "https://github.com/phpdot/monorepo/issues"
    }
}
